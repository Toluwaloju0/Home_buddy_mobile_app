This directory contains all the databases collections names and document format.

Mongoose is used to ensure that all fields which are required are present and filled when creating a new document in the database